[package]
name = "userland-compositor"
version = "0.1.0"
edition = "2021"
description = "Compositor: transparency, rounded corners, composite via DisplayBackend"

[lib]

[dependencies]
userland-core = { path = "../core" }
