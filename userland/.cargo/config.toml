# Static relocation so ELF is not PIE; kernel loader maps at p_vaddr and does not apply relocations.
[target.x86_64-unknown-none]
rustflags = ["-C", "relocation-model=static", "-C", "link-arg=-no-pie"]
